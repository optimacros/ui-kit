import * as toast from '@zag-js/toast';

export const props: toast.StoreProps = {
    gap: 16,
    max: Number.MAX_SAFE_INTEGER,
    overlap: false,
    duration: 999999,
    removeDelay: 0,
    placement: 'bottom',
};
