export { Icon, type IconProps } from '@optimacros-ui/icon';
import { Icon, type IconProps } from '@optimacros-ui/icon';

export const WSIcon = (props: IconProps) => <Icon width="15" height="15" {...props} />;
